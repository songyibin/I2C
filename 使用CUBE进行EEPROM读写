#用CUBE建立工程
在模板选项里选择所用的板子，NUCLEO F334R8，它就会给您一个最小系统的pin配置。把USART的两个脚去掉，然后再时钟树里面设置频率为72MHz，为最快的。

在Project选项卡里选择配置编译环境为MDK-ARM，在Code 
Generation中Generated Files的第一个框勾上，这样能针对不同外设生成相应的.c和.h文件，可读性会强些。在最后一个选项卡中选择使用LL库，这样编译速度会快些。


